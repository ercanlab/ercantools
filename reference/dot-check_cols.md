# Validate that required columns exist in a data frame

Validate that required columns exist in a data frame

## Usage

``` r
.check_cols(object, cols, arg_names = cols)
```

## Arguments

- object:

  A data.frame or tibble.

- cols:

  Character vector of required column names.

- arg_names:

  Character vector of argument names for the error message.
