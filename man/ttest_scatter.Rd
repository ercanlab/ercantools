% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ttest_scatter.R
\name{ttest_scatter}
\alias{ttest_scatter}
\title{Dot plot of -log10(p-value) for log2 fold-change comparisons along chromosomes}
\usage{
ttest_scatter(
  object = NULL,
  title = NULL,
  subtitle = NULL,
  ylab = "-log10(p-value)",
  fc_col = "Fold",
  chr_col = "seqnames",
  autosomes = c("chrI", "chrII", "chrIII", "chrIV", "chrV"),
  chr_X = "chrX",
  chr_colors = c(chrI = "#66C2A5", chrII = "#FC8D62", chrIII = "#8DA0CB", chrIV =
    "#E78AC3", chrV = "#A6D854", chrX = "#EF9A9A"),
  pvalue_threshold_y = -log10(0.05),
  pvalue_y_position_offset = 0.5,
  pvalue_x_position = 6,
  output_pdf = NULL,
  pdf_width = 8,
  pdf_height = 6
)
}
\arguments{
\item{object}{A \code{data.frame} or \code{tibble}.}

\item{title}{Character. Main plot title. Default \code{NULL}.}

\item{subtitle}{Character. Plot subtitle. Default \code{NULL}.}

\item{ylab}{Character. Y-axis label. Default \code{"-log10(p-value)"}.}

\item{fc_col}{Character. Column name for log2 fold-change. Default \code{"Fold"}.}

\item{chr_col}{Character. Chromosome column name. Default \code{"seqnames"}.}

\item{autosomes}{Character vector. Autosome names.}

\item{chr_X}{Character. Sex chromosome name. Default \code{"chrX"}.}

\item{chr_colors}{Named character vector. Colors per chromosome.}

\item{pvalue_threshold_y}{Numeric. Significance threshold on -log10 scale.
Default \code{-log10(0.05)}.}

\item{pvalue_y_position_offset}{Numeric. Vertical offset (in plot units) for placing
the p-value threshold label above the horizontal reference line.  Default: \code{0.5}.}

\item{pvalue_x_position}{Numeric. Absolute X position (in plot units)
for p-value threshold label, right of the plot. Default \code{6}.}

\item{output_pdf}{Character. Optional PDF output path. Default \code{NULL}.}

\item{pdf_width}{Numeric. Default \code{8}.}

\item{pdf_height}{Numeric. Default \code{6}.}
}
\value{
A named list with \code{plot} and \code{results}.
}
\description{
For each chromosome, runs a Welch t-test on log2 fold-change values
("Autosome vs Autosomes" and "X vs Autosomes") and displays results as a dot plot.
}
\examples{
\dontrun{
res <- ttest_scatter(
  object = my_peaks,
  title  = "FC t-test (Autosome vs Autosomes and X vs Autosomes)"
)
res$plot
}

}
