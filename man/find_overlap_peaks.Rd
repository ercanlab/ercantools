% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/peak_utils.R
\name{find_overlap_peaks}
\alias{find_overlap_peaks}
\title{Find overlapping peaks between two peak sets}
\usage{
find_overlap_peaks(df1, df2, bp_distance)
}
\arguments{
\item{df1}{A \code{data.frame} with at least \code{seqnames}, \code{start}, and \code{end} columns.}

\item{df2}{A \code{data.frame} with the same requirements as \code{df1}.}

\item{bp_distance}{Numeric. Half-width in base pairs of the window around
each peak midpoint used for overlap detection.}
}
\value{
A named list with three elements:
\itemize{
\item \code{df1}: input \code{df1} with an added \code{has_overlap_peak} logical column.
\item \code{df2}: input \code{df2} with an added \code{has_overlap_peak} logical column.
\item \code{df2_percentage}: Character string reporting the percentage of \code{df2}
peaks that overlap \code{df1} (e.g., \code{"72.34\% (362/500)"}).
}
}
\description{
Expands each peak to a window of \verb{mid ± bp_distance} (where
\code{mid = (start + end) / 2}) and uses \code{\link[GenomicRanges:findOverlaps-methods]{GenomicRanges::findOverlaps()}}
to identify which peaks in each set overlap with the other.
A logical \code{has_overlap_peak} column is appended to both data frames.
}
\examples{
\dontrun{
data(example_peaks)
peaks_a <- example_peaks[example_peaks$seqnames == "chrX", ]
peaks_b <- example_peaks[example_peaks$seqnames \%in\% c("chrX", "chrI"), ]

res <- find_overlap_peaks(peaks_a, peaks_b, bp_distance = 100)
cat(res$df2_percentage)
head(res$df1)
}

}
