% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_distributions.R
\name{plot_distributions}
\alias{plot_distributions}
\title{Density or histogram of normalized binding counts per chromosome}
\usage{
plot_distributions(
  object,
  title = NULL,
  subtitle = NULL,
  chromosomes = .elegans_chr_order(),
  conc_cols = c(Conc_base = "Conc_notag", Conc_condition = "Conc_degron"),
  condition = "Degron",
  baseline = "NoTag",
  type = c("density", "hist"),
  overlay = TRUE,
  ncol = 3,
  free_y = TRUE,
  add_rug = FALSE
)
}
\arguments{
\item{object}{A \code{data.frame} or \code{tibble}. Must contain a \code{seqnames} column.}

\item{title}{Character. Plot title. Default \code{NULL}.}

\item{subtitle}{Character. Plot subtitle. Default \code{NULL}.}

\item{chromosomes}{Character vector. Chromosomes to include.
Default is C. elegans order.}

\item{conc_cols}{Named character vector mapping role to column name.
Default \code{c(Conc_base = "Conc_notag", Conc_condition = "Conc_degron")}.}

\item{condition}{Character. Display name for condition. Default \code{"Degron"}.}

\item{baseline}{Character. Display name for baseline. Default \code{"NoTag"}.}

\item{type}{Character. \code{"density"} (default) or \code{"hist"}.}

\item{overlay}{Logical. If \code{TRUE} (default), conditions are overlaid within
each chromosome facet. If \code{FALSE}, a \code{Condition ~ seqnames} grid is used.}

\item{ncol}{Integer. Number of facet columns. Default \code{3}.}

\item{free_y}{Logical. If \code{TRUE} (default), facet y-axes are free.}

\item{add_rug}{Logical. Add a rug to the x-axis? Default \code{FALSE}.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object.
}
\description{
Plots the distribution of normalized binding counts for two experimental
conditions, faceted by chromosome. Supports density and histogram
geometries, as well as overlaid or grid facetting.
}
\examples{
\dontrun{
plot_distributions(
  object    = my_peaks,
  title     = "Binding count distributions",
  condition = "Degron",
  baseline  = "NoTag"
)
}

}
