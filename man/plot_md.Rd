% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_md.R
\name{plot_md}
\alias{plot_md}
\title{MA / MD plot for differential binding results}
\usage{
plot_md(
  object,
  x_var = "Conc",
  y_var = "Fold",
  fdr_var = "FDR",
  cutoff_y = 1,
  cutoff_FDR = 0.05,
  y_max = 3,
  y_min = -3,
  label_var = NULL,
  peaks = NULL,
  title = "MD Plot",
  colors = c("red", "grey70", "blue")
)
}
\arguments{
\item{object}{A \code{data.frame} or \code{tibble} — typically the output of
\code{DiffBind::dba.report()} coerced to a data frame.}

\item{x_var}{Character. Column with average abundance(normalized binding counts). Default \code{"Conc"}.}

\item{y_var}{Character. Column with log2 fold-change. Default \code{"Fold"}.}

\item{fdr_var}{Character. Column with FDR values. Default \code{"FDR"}.}

\item{cutoff_y}{Numeric. log2FC threshold for coloring. Default \code{1}.}

\item{cutoff_FDR}{Numeric. FDR threshold for coloring. Default \code{0.05}.}

\item{y_max}{Numeric. Upper y-axis limit. Default \code{3}.}

\item{y_min}{Numeric. Lower y-axis limit. Default \code{-3}.}

\item{label_var}{Character. Column used for peak labels. Default \code{NULL}.}

\item{peaks}{Character vector. Peak IDs to label. Default \code{NULL}.}

\item{title}{Character. Plot title. Default \code{"MD Plot"}.}

\item{colors}{Character vector of length 3. Colors for up-regulated,
no-change, and down-regulated points.
Default \code{c("red", "grey70", "blue")}.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object.
}
\description{
Plots log2 fold-change (y-axis) against average abundance/normalized binding counts
(x-axis), with points coloured by significance status. Optionally labels
a user-provided set of peaks with \code{\link[ggrepel:geom_text_repel]{ggrepel::geom_label_repel()}}.
}
\examples{
\dontrun{
plot_md(
  object  = my_peaks,
  x_var   = "Conc",
  y_var   = "Fold",
  fdr_var = "FDR",
  title   = "MD plot — degron vs notag"
)
}

}
