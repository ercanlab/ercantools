% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/peak_utils.R
\name{upset_peaks}
\alias{upset_peaks}
\title{UpSet plot for peak category overlaps}
\usage{
upset_peaks(
  object,
  sets = NULL,
  set_labels = NULL,
  order_by = "freq",
  set_colors = c("#E41A1C", "#377EB8", "#4DAF4A", "#984EA3", "#FF7F00", "#A65628"),
  title = NULL,
  output_pdf = NULL,
  width = 12,
  height = 8
)
}
\arguments{
\item{object}{A \code{data.frame} with logical columns named \verb{has_overlap_*}.}

\item{sets}{Character vector. Column names to include. If \code{NULL} (default),
all \verb{has_overlap_*} columns are used automatically.}

\item{set_labels}{Character vector. Display labels for each set.
If \code{NULL}, labels are derived by removing the \code{has_overlap_} prefix.}

\item{order_by}{Character. \code{"freq"} (default) or \code{"degree"}.}

\item{set_colors}{Character vector. Bar colours for each set.}

\item{title}{Character. Optional title above the plot. Default \code{NULL}.}

\item{output_pdf}{Character. Optional path to save PDF. Default \code{NULL}.}

\item{width}{Numeric. Plot width in inches. Default \code{12}.}

\item{height}{Numeric. Plot height in inches. Default \code{8}.}
}
\value{
Invisibly returns the UpSet plot object.
}
\description{
Creates an \code{\link[UpSetR:upset]{UpSetR::upset()}} plot from a peak data frame containing
logical \verb{has_overlap_*} columns produced by repeated calls to
\code{\link[=find_overlap_peaks]{find_overlap_peaks()}}. Any column matching \code{has_overlap_} is
automatically detected if \code{sets} is \code{NULL}.
}
\examples{
\dontrun{
data(example_peaks)
example_peaks$has_overlap_promoter <- sample(c(TRUE, FALSE),
                                             nrow(example_peaks),
                                             replace = TRUE)
example_peaks$has_overlap_enhancer <- sample(c(TRUE, FALSE),
                                             nrow(example_peaks),
                                             replace = TRUE)
upset_peaks(example_peaks, title = "Peak category overlaps")
}

}
