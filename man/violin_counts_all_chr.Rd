% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/violin_all_chr.R
\name{violin_counts_all_chr}
\alias{violin_counts_all_chr}
\title{Violin + box plot for all chromosomes separately (normalized binding counts)}
\usage{
violin_counts_all_chr(
  object = NULL,
  title = NULL,
  subtitle = NULL,
  ylab = "log2(Normalized binding counts)",
  xlab = NULL,
  conc_condition = "Conc_degron",
  condition_name = "degron",
  conc_baseline = "Conc_notag",
  baseline_name = "notag",
  chr_col = "seqnames",
  chr_of_interest = "chrX",
  ylim = c(0, 12),
  chromosomes = .elegans_chr_order(),
  color_condition = "#ff6347ff",
  color_baseline = "#4682b4ff",
  output_pdf = NULL,
  pdf_width = 14,
  pdf_height = 7
)
}
\arguments{
\item{object}{A \code{data.frame} or \code{tibble}.}

\item{title}{Character. Main plot title. Default \code{NULL}.}

\item{subtitle}{Character. Plot subtitle. Default \code{NULL}.}

\item{ylab}{Character. Y-axis label. Default \verb{log2(Normalized binding counts)}.}

\item{xlab}{Character. X-axis label. Default \code{NULL}.}

\item{conc_condition}{Character. Column name for condition counts. Default \code{"Conc_degron"}.}

\item{condition_name}{Character. Display name for condition. Default \code{"degron"}.}

\item{conc_baseline}{Character. Column name for baseline counts. Default \code{"Conc_notag"}.}

\item{baseline_name}{Character. Display name for baseline. Default \code{"notag"}.}

\item{chr_col}{Character. Chromosome column name. Default \code{"seqnames"}.}

\item{chr_of_interest}{Character. Chromosome to highlight. Default \code{"chrX"}.}

\item{ylim}{Numeric vector of length 2. Default \code{c(0, 12)}.}

\item{chromosomes}{Character vector. Chromosomes included and their order.}

\item{color_condition}{Character. Fill colour for condition. Default \code{"#ff6347ff"}.}

\item{color_baseline}{Character. Fill colour for baseline. Default \code{"#4682b4ff"}.}

\item{output_pdf}{Character. Optional path to save plot as PDF. Default \code{NULL}.}

\item{pdf_width}{Numeric. Default \code{14}.}

\item{pdf_height}{Numeric. Default \code{7}.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object.
}
\description{
For each chromosome, draws paired violins for two experimental conditions (ex. degron/notag)
and compares them with \code{\link[ggpubr:stat_compare_means]{ggpubr::stat_compare_means()}}.
}
\examples{
\dontrun{
violin_counts_all_chr(
  object           = my_peaks,
  title            = "Normalized counts per chromosome",
  conc_condition   = "Conc_degron",
  conc_baseline    = "Conc_notag"
)
}

}
